# 版本規則

本專案採用 [Semantic Versioning 2.0.0](https://semver.org/lang/zh-TW/)
作為公開漢化包版本規則。

SemVer 對 `0.y.z` 初始開發階段允許較大的變動；本專案為了讓一般玩家、
問題回報與檔案雜湊保持可追溯，採用更嚴格的規則：

> 任何已公開的版本，包括 `0.x` 與 prerelease，內容與 Release assets
> 都不可靜默替換。所有修改都必須使用新版本號。

## 公開版本格式

目前公開流程支援：

- Beta：`MAJOR.MINOR.PATCH-beta.N`，例如 `0.8.0-beta.1`。
- Stable：`MAJOR.MINOR.PATCH`，例如 `1.0.0`。

其中：

- `MAJOR`、`MINOR`、`PATCH` 不得有前導零。
- `beta.N` 的 `N` 必須是從 `1` 開始的正整數，且不得有前導零。
- Stable 不得包含 prerelease identifier。
- Stable 的 `MAJOR` 必須至少為 `1`；0.x 一律視為 prerelease 階段。
- Beta 必須包含 `-beta.N`。
- 公開版本禁止使用 `+BUILD` metadata。建置工具版本、資安工具版本、
  Git commit 與檔案 SHA-256 必須記在 Release notes 與驗證檔，不放進
  玩家版本號。

漢化包、非公開建置工具與 Release 資安工具各自獨立編版。例如：

```text
漢化包：0.8.0-beta.1
建置工具：1.3.0
資安工具：1.0.0
```

## 相容性契約

本專案的 SemVer「相容性」不是 API，而是下列玩家與協作者可觀察契約：

- 支援的遊戲版本、平台與原廠 PAK SHA-256。
- 安裝、升級、移除、備份與失敗回滾流程。
- 舊版漢化安裝狀態能否安全辨識與遷移。
- 遊戲存檔是否保持相容。
- 玩家可見的翻譯範圍、字型與必要顯示行為。
- 公開譯文格式及貢獻流程是否維持相容。

只有改變內部建置實作、但上述契約維持不變時，不構成產品版本的破壞性
變更。

## 草稿、公開與不可變

- GitHub Release 維持 Draft 時，可以在完整重建及重新驗證後替換 assets，
  版本號可維持不變。
- 下列任一事件先發生時，該版本即視為已公開：
  1. Git tag 出現在公開 remote。
  2. GitHub Release 不再是 Draft。
- 已公開版本的 tag、ZIP、index、security audit、Release notes 與其他
  assets 均不可用不同內容覆蓋。
- 即使尚無已知下載，也不得把相同版本刪除後用不同內容重發。
- 若公開版本有嚴重問題，應將 Release 標示為 withdrawn／有問題，說明
  原因及替代版本，再發布遞增後的新版本。

## 升版規則

### 1.0.0 之前

| 變更 | 版本 |
|---|---|
| 同一 Beta 範圍內的翻譯、安裝器、安全或相容性修正 | 遞增 `beta.N`，例如 `0.8.0-beta.2` |
| 有意擴大產品範圍、支援基線或玩家可見能力 | 遞增 `MINOR` 並重設 Beta，例如 `0.9.0-beta.1` |
| 發現原本承諾範圍內的漏翻或錯譯 | 視為修正，遞增 `beta.N`，不自動升 `MINOR` |
| 0.x 階段的不相容調整 | 遞增下一個 `MINOR` prerelease，不跳至 `2.0.0` |
| 達成本文的 Stable 退出標準 | `1.0.0` |

### 1.0.0 之後

| 變更 | 版本 |
|---|---|
| 相容的翻譯、安裝器或安全修正 | 遞增 `PATCH`，例如 `1.0.1` |
| 相容的新翻譯範圍、平台或功能 | 遞增 `MINOR`，例如 `1.1.0` |
| 破壞相容性契約 | 遞增 `MAJOR`，例如 `2.0.0` |

改用不同 patcher 或安裝器實作不會自動升 MAJOR；只有舊版無法安全升級、
移除或回滾，支援基線不相容，或玩家必須手動清除舊狀態時，才屬於破壞性
變更。大幅改寫譯文或術語通常是 MINOR，除非同時破壞上述相容性契約。

## 1.0.0 Stable 退出標準

只有全部符合時才可發布 `1.0.0`：

1. 目前宣告範圍內的文字槽均已分類、完成翻譯或有公開理由排除；正式驗證
   未發現已知的玩家可見英文殘留。
2. 所有發布 PAK 均通過結構、內容、字型、差分 round-trip 與必要 runtime
   smoke tests。
3. 啟動、模式進入、序章與對話、存讀檔、確認視窗、名單、商店名稱、
   關鍵道具標題及整體文字品質等必要測項全部通過。
4. 安裝、移除、備份、失敗回滾及未知原廠 PAK 拒絕機制全部通過。
5. Release security audit 為 `passed`、`findings=[]`，且 Release 不含
   完整遊戲 PAK、金鑰、憑證或私人建置資訊。
6. Windows 10 與 Windows 11 各至少有一個獨立、乾淨遊戲環境完成安裝、
   遊戲啟動與移除驗證。
7. 至少一個公開 Beta 維持 14 個日曆日，期間沒有尚未關閉的 blocker。

Blocker 包含：

- 遊戲 crash、無法進入必要模式或存檔損壞。
- 安裝失敗後不能安全回滾，或移除後不能恢復原廠檔案。
- `<Region ID not fount>`、空白必要提示、方框字、關鍵文字截斷。
- 已知玩家可見英文殘留仍在宣告翻譯範圍內。
- 安裝器接受未知或已被其他 MOD 修改的 PAK。
- Release 含完整遊戲檔案、憑證、金鑰或私人資訊。

## Release notes 必填資訊

每個公開版本至少記錄：

- 漢化包版本與 prerelease／stable 狀態。
- 對應的公開譯文 commit。
- 建置工具與資安工具版本。
- ZIP、release index 與 security audit 的 SHA-256。
- 支援的遊戲版本／原廠基線。
- 玩家可見變更、已知問題及升級／移除注意事項。
- 若撤回舊版，撤回原因與替代版本。
